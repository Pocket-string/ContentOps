# Supabase — Required
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase — Optional (admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI — Gemini (Google AI Studio) — Primary AI provider
GOOGLE_AI_API_KEY=your-google-ai-studio-key

# AI — OpenAI (ChatGPT) — Prompt generator + content reviewer
OPENAI_API_KEY=your-openai-api-key

# AI — OpenRouter — Legacy fallback (optional)
OPENROUTER_API_KEY=your-openrouter-key

# App — Optional
NEXT_PUBLIC_SITE_URL=http://localhost:3000
