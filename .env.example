# Supabase — Required
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase — Optional (admin operations)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# AI — Gemini (Google AI Studio) — Optional if users bring their own keys (BYOK)
GOOGLE_AI_API_KEY=your-google-ai-studio-key

# AI — OpenAI (ChatGPT) — Optional (content reviewer)
OPENAI_API_KEY=your-openai-api-key

# AI — OpenRouter — Optional (fallback provider)
OPENROUTER_API_KEY=your-openrouter-key

# BYOK — Required for encrypting user API keys (32 bytes hex, generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
API_KEY_ENCRYPTION_SECRET=your-64-char-hex-secret

# App — Optional
NEXT_PUBLIC_SITE_URL=http://localhost:3000
